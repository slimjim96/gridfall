# .NET
bin/
obj/
*.user

# Godot
.godot/
.import/
export_presets.cfg
*.translation

# Generated from content-data JSON — never hand-edited, never committed
godot/resources/generated/

# Determinism traces ARE committed. They are the regression fixtures -- `Verify replay`
# has nothing to check without them, and a trace that only exists on one machine
# cannot catch a platform divergence, which is most of what it is for.

# OS
.DS_Store
Thumbs.db

# Godot build/import artifacts
godot/.godot/
godot/.mono/
